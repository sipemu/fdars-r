% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/spm.R
\name{spm.profile.phase1}
\alias{spm.profile.phase1}
\title{Profile Monitoring Phase I}
\usage{
spm.profile.phase1(
  fdataobj,
  predictors,
  ncomp = 3,
  alpha = 0.05,
  fosr.lambda = 1e-04,
  window.size = 20,
  step.size = 1
)
}
\arguments{
\item{fdataobj}{An object of class \code{fdata} (functional response).}

\item{predictors}{A matrix of scalar predictors (n x p).}

\item{ncomp}{Number of principal components for beta FPCA (default 3).}

\item{alpha}{Significance level (default 0.05).}

\item{fosr.lambda}{FOSR smoothing parameter (default 1e-4).}

\item{window.size}{Window size for sliding-window FOSR (default 20).}

\item{step.size}{Step size between windows (default 1).}
}
\value{
An object of class \code{spm.profile.chart} with components:
\describe{
\item{eigenvalues}{Eigenvalues from beta FPCA}
\item{t2.ucl}{T-squared control limit}
\item{t2.description}{Description of the limit}
\item{lag1.autocorrelation}{Lag-1 autocorrelation of T-squared values}
\item{effective.n.windows}{Effective number of independent windows}
\item{fdataobj}{Original fdata object}
\item{predictors}{Original predictor matrix}
\item{.rust}{Internal fields for Phase II monitoring}
}
}
\description{
Builds a profile monitoring chart for functional data with scalar covariates.
Uses function-on-scalar regression (FOSR) to model the relationship, then
applies FPCA to the regression coefficients across sliding windows.
}
\examples{
\donttest{
set.seed(1)
n <- 80; m <- 20
argvals <- seq(0, 1, length.out = m)
X_pred <- cbind(rnorm(n), rnorm(n))
Y <- matrix(rnorm(n * m), n, m)
fd <- fdata(Y, argvals = argvals)

chart <- spm.profile.phase1(fd, X_pred, ncomp = 2, window.size = 15)
chart
}

}
\seealso{
\code{\link{spm.profile.monitor}} for Phase II monitoring
}
