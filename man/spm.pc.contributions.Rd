% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/spm.R
\name{spm.pc.contributions}
\alias{spm.pc.contributions}
\title{Per-PC Contributions to T-Squared Statistic}
\usage{
spm.pc.contributions(scores, eigenvalues)
}
\arguments{
\item{scores}{Score matrix (n x ncomp), e.g., from \code{spm.monitor()$scores}.}

\item{eigenvalues}{Numeric vector of eigenvalues (length ncomp).}
}
\value{
A matrix (n x ncomp) of per-component T-squared contributions.
Row sums equal the T-squared statistics.
}
\description{
Computes the contribution of each principal component to the Hotelling
T-squared statistic for each observation. Useful for diagnosing which
components drive an alarm.
}
\examples{
\donttest{
set.seed(1)
scores <- matrix(rnorm(10 * 3), 10, 3)
eigenvalues <- c(5, 2, 1)
contrib <- spm.pc.contributions(scores, eigenvalues)
dim(contrib)  # 10 x 3
}

}
\seealso{
\code{\link{spm.contributions}} for per-variable contributions
}
