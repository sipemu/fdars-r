% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/spm.R
\name{spm.amewma}
\alias{spm.amewma}
\title{Adaptive MEWMA (AMEWMA) Monitoring for Functional Data}
\usage{
spm.amewma(
  chart,
  newdata,
  lambda.min = 0.05,
  lambda.max = 0.95,
  lambda.init = 0.2,
  eta = 0.1,
  ncomp = NULL,
  alpha = 0.05
)
}
\arguments{
\item{chart}{An object of class \code{spm.chart} from \code{\link{spm.phase1}}.}

\item{newdata}{An object of class \code{fdata} with sequential observations.}

\item{lambda.min}{Minimum smoothing parameter (default 0.05).}

\item{lambda.max}{Maximum smoothing parameter (default 0.95).}

\item{lambda.init}{Initial smoothing parameter (default 0.2).}

\item{eta}{Learning rate for lambda adaptation (default 0.1).}

\item{ncomp}{Number of components (default: same as chart).}

\item{alpha}{Significance level (default 0.05).}
}
\value{
An object of class \code{spm.amewma} with components:
\describe{
\item{smoothed.scores}{Matrix of adaptively smoothed score vectors}
\item{t2.statistic}{Numeric vector of T-squared statistics on smoothed scores}
\item{lambda.t}{Numeric vector of adaptive lambda values over time}
\item{ucl}{Control limit}
\item{alarm}{Logical vector: TRUE where statistic exceeds UCL}
}
}
\description{
Applies Adaptive Multivariate EWMA monitoring where the smoothing parameter
adapts over time based on the magnitude of recent deviations. This provides
good detection performance for both small and large shifts.
}
\examples{
\donttest{
set.seed(1)
n <- 50; m <- 30
argvals <- seq(0, 1, length.out = m)
X <- matrix(rnorm(n * m), n, m)
fd <- fdata(X, argvals = argvals)
chart <- spm.phase1(fd, ncomp = 3)

X_new <- matrix(rnorm(30 * m) + 0.5, 30, m)
fd_new <- fdata(X_new, argvals = argvals)
amewma <- spm.amewma(chart, fd_new)
amewma
}

}
\seealso{
\code{\link{spm.mewma}} for fixed-lambda MEWMA,
\code{\link{spm.ewma}} for univariate EWMA
}
