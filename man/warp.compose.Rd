% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/alignment.R
\name{warp.compose}
\alias{warp.compose}
\title{Compose Two Warping Functions}
\usage{
warp.compose(gamma1, gamma2, fdataobj)
}
\arguments{
\item{gamma1}{Numeric vector — outer warping function.}

\item{gamma2}{Numeric vector — inner warping function.}

\item{fdataobj}{An fdata object providing the grid (argvals).}
}
\value{
Numeric vector of the composed warping function.
}
\description{
Computes the composition (gamma1 o gamma2)(t) = gamma1(gamma2(t)).
}
\examples{
\donttest{
t <- seq(0, 1, length.out = 20)
gamma1 <- t^2
gamma2 <- sqrt(t)
composed <- warp.compose(gamma1, gamma2, t)
}

}
