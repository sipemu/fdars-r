% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/explain.R
\name{fregre.anchor}
\alias{fregre.anchor}
\title{Anchor Explanation}
\usage{
fregre.anchor(model, data, observation, precision = 0.95, n.bins = 10)
}
\arguments{
\item{model}{A fitted \code{fregre.lm} or \code{fregre.logistic} model.}

\item{data}{An fdata object.}

\item{observation}{Index of the observation (1-based).}

\item{precision}{Minimum precision threshold (default 0.95).}

\item{n.bins}{Number of bins for discretizing scores (default 10).}
}
\value{
A list with conditions, precision, coverage, n_matching,
observation, and predicted_value.
}
\description{
Finds a rule (set of FPC score conditions) that anchors the prediction
to a narrow range with high precision.
}
\examples{
\donttest{
fd <- fdata(matrix(rnorm(500), nrow = 50), argvals = seq(0, 1, length.out = 10))
y <- rnorm(50)
fit <- fregre.lm(fd, y, ncomp = 3)
result <- fregre.anchor(fit, fd, observation = 1)
}
}
