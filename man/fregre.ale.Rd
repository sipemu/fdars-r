% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/explain.R
\name{fregre.ale}
\alias{fregre.ale}
\title{Accumulated Local Effects}
\usage{
fregre.ale(model, data, component, n.bins = 20)
}
\arguments{
\item{model}{A fitted \code{fregre.lm} or \code{fregre.logistic} model.}

\item{data}{An fdata object.}

\item{component}{Which FPC component (1-based).}

\item{n.bins}{Number of ALE bins (default 20).}
}
\value{
A list with bin_midpoints, ale_values, bin_edges,
bin_counts, and component.
}
\description{
Computes ALE for a specific FPC component.
}
\examples{
\donttest{
fd <- fdata(matrix(rnorm(500), nrow = 50), argvals = seq(0, 1, length.out = 10))
y <- rnorm(50)
fit <- fregre.lm(fd, y, ncomp = 3)
result <- fregre.ale(fit, fd, component = 1)
}
}
