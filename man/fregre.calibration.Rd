% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/explain.R
\name{fregre.calibration}
\alias{fregre.calibration}
\title{Calibration Diagnostics (Logistic)}
\usage{
fregre.calibration(model, y, n.groups = 10)
}
\arguments{
\item{model}{A fitted functional logistic model.}

\item{y}{True binary labels (0/1).}

\item{n.groups}{Number of groups for reliability diagram (default 10).}
}
\value{
A list with brier_score, log_loss, hosmer_lemeshow_chi2,
hosmer_lemeshow_df, n_groups, reliability_bins, and bin_counts.
}
\description{
Computes calibration diagnostics for a functional logistic model:
Brier score, log loss, and Hosmer-Lemeshow test.
}
\examples{
\donttest{
fd <- fdata(matrix(rnorm(500), nrow = 50), argvals = seq(0, 1, length.out = 10))
y_bin <- factor(sample(0:1, 50, replace = TRUE))
fit <- functional.logistic(fd, y_bin)
result <- fregre.calibration(fit, as.numeric(as.character(y_bin)))
}
}
