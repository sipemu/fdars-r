% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/spm.R
\name{spm.monitor.partial.batch}
\alias{spm.monitor.partial.batch}
\title{Monitor a Batch of Partially-Observed Curves}
\usage{
spm.monitor.partial.batch(
  chart,
  partial.data.list,
  n.observed.list,
  completion = c("conditional", "projection", "zero_pad"),
  ncomp = NULL,
  alpha = 0.05
)
}
\arguments{
\item{chart}{An object of class \code{spm.chart} from \code{\link{spm.phase1}}.}

\item{partial.data.list}{A list of numeric vectors, each containing the
observed values for one curve.}

\item{n.observed.list}{An integer vector giving the number of observed
grid points for each curve.}

\item{completion}{Completion method: \code{"conditional"}, \code{"projection"},
or \code{"zero_pad"}.}

\item{ncomp}{Number of components (default: same as chart).}

\item{alpha}{Significance level (default 0.05).}
}
\value{
A list of results, one per curve. Each element is a list with:
\item{scores}{Estimated FPC scores}
\item{t2}{T-squared statistic}
\item{t2.alarm}{Logical}
\item{domain.fraction}{Fraction of domain observed}
\item{completed.curve}{The completed curve, or NULL}
}
\description{
Monitors multiple partially-observed curves in a single call. Each curve
may have a different number of observed grid points.
}
\examples{
\donttest{
set.seed(1)
n <- 50; m <- 30
argvals <- seq(0, 1, length.out = m)
X <- matrix(rnorm(n * m), n, m)
fd <- fdata(X, argvals = argvals)
chart <- spm.phase1(fd, ncomp = 3)

# Three curves with different observed lengths
curves <- list(rnorm(30), rnorm(30), rnorm(30))
n_obs <- c(10L, 20L, 25L)
results <- spm.monitor.partial.batch(chart, curves, n_obs)
sapply(results, function(r) r$t2)
}

}
\seealso{
\code{\link{spm.monitor.partial}} for a single curve
}
