% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/alignment.R
\name{elastic.pair.penalized}
\alias{elastic.pair.penalized}
\title{Penalized Elastic Pairwise Alignment}
\usage{
elastic.pair.penalized(
  f1,
  f2,
  argvals = NULL,
  penalty = c("first_order", "second_order", "combined"),
  lambda = 1
)
}
\arguments{
\item{f1}{Numeric vector (reference curve).}

\item{f2}{Numeric vector (curve to align).}

\item{argvals}{Numeric vector of evaluation points. If \code{NULL},
defaults to \code{seq(0, 1, length.out = length(f1))}.}

\item{penalty}{Character: penalty type. One of \code{"first_order"},
\code{"second_order"}, or \code{"combined"}.}

\item{lambda}{Numeric penalty weight (default 1.0).}
}
\value{
A list with components:
\describe{
\item{gamma}{Warping function}
\item{f.aligned}{Aligned version of f2}
\item{distance}{Elastic distance after alignment}
}
}
\description{
Aligns curve \code{f2} to curve \code{f1} using elastic alignment with
a configurable roughness penalty on the warping function.
}
\examples{
\donttest{
t <- seq(0, 1, length.out = 50)
f1 <- sin(2 * pi * t)
f2 <- sin(2 * pi * t^1.5)
res <- elastic.pair.penalized(f1, f2, argvals = t)
}
}
\seealso{
\code{\link{elastic.pair}}, \code{\link{elastic.lambda.cv}}
}
