% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/conformal.R
\name{jackknife.plus}
\alias{jackknife.plus}
\title{Jackknife+ Regression}
\usage{
jackknife.plus(
  fdataobj,
  y,
  newdata,
  method = c("fregre.lm", "fregre.np"),
  scalar.train = NULL,
  scalar.test = NULL,
  ncomp = 3,
  h.func = 0,
  h.scalar = 0,
  alpha = 0.1,
  seed = NULL
)
}
\arguments{
\item{fdataobj}{An object of class 'fdata' (training data).}

\item{y}{Response vector (training).}

\item{newdata}{An object of class 'fdata' (test data).}

\item{method}{Regression method: "fregre.lm" or "fregre.np".}

\item{scalar.train}{Optional scalar covariates for training.}

\item{scalar.test}{Optional scalar covariates for test.}

\item{ncomp}{Number of FPC components (default 3, for fregre.lm).}

\item{h.func}{Functional bandwidth (default 0 = auto, for fregre.np).}

\item{h.scalar}{Scalar bandwidth (default 0 = auto, for fregre.np).}

\item{alpha}{Miscoverage level (default 0.1 for 90 percent intervals).}

\item{seed}{Random seed (unused, kept for API consistency).}
}
\value{
Same as \code{\link{cv.conformal.regression}}.
}
\description{
Constructs prediction intervals using jackknife+ (leave-one-out conformal).
Most sample-efficient but most expensive method (n refits).
}
\examples{
\donttest{
fd <- fdata(matrix(rnorm(500), 50, 10), argvals = seq(0, 1, length.out = 10))
y <- rnorm(50)
jp <- jackknife.plus(fd[1:40, ], y[1:40], fd[41:50, ])
}

}
