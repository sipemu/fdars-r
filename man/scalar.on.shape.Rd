% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/scalar_on_shape.R
\name{scalar.on.shape}
\alias{scalar.on.shape}
\title{Scalar-on-Shape Regression}
\usage{
scalar.on.shape(
  fdataobj,
  y,
  nbasis = 11,
  lambda = 0.001,
  index.method = c("identity", "polynomial", "nadaraya.watson"),
  index.param = 2,
  g.degree = 2,
  dp.lambda = 0,
  max.iter.outer = 10,
  max.iter.inner = 15,
  tol = 1e-04
)
}
\arguments{
\item{fdataobj}{An object of class 'fdata'.}

\item{y}{Scalar response vector (length n).}

\item{nbasis}{Number of Fourier basis functions for beta (default 11).}

\item{lambda}{Roughness penalty weight (default 1e-3).}

\item{index.method}{Index function method: "identity" (default),
"polynomial", or "nadaraya.watson".}

\item{index.param}{Parameter for the index method: polynomial degree
(integer) or Nadaraya-Watson bandwidth (numeric). Default 2.}

\item{g.degree}{Polynomial degree for the amplitude link g (default 2).}

\item{dp.lambda}{Dynamic programming alignment penalty (default 0).}

\item{max.iter.outer}{Maximum outer iterations (default 10).}

\item{max.iter.inner}{Maximum inner iterations (default 15).}

\item{tol}{Convergence tolerance (default 1e-4).}
}
\value{
An object of class 'scalar.on.shape' (a list) with components:
\item{beta}{Estimated shape index function (length m).}
\item{beta.coefficients}{Fourier coefficients for beta.}
\item{gammas}{Warping functions (n x m matrix).}
\item{shape.scores}{Shape scores (length n).}
\item{h.coefficients}{Index link function coefficients.}
\item{g.coefficients}{Amplitude link function coefficients.}
\item{fitted.values}{Fitted response values.}
\item{residuals}{Residuals.}
\item{sse}{Residual sum of squares.}
\item{r.squared}{Coefficient of determination.}
\item{n.iter.outer}{Number of outer iterations.}
\item{n.iter.inner}{Number of inner iterations.}
\item{index.method}{Index method used.}
\item{index.param}{Index method parameter.}
\item{fdataobj}{Training fdata object.}
\item{y}{Training response.}
}
\description{
Fits a scalar-on-shape regression model where the response depends on the
\emph{shape} of the functional predictor, with phase variation removed via
elastic alignment (Fisher-Rao framework).
}
\examples{
\donttest{
fd <- fdata(matrix(rnorm(500), 50, 10), argvals = seq(0, 1, length.out = 10))
y <- rnorm(50)
fit <- scalar.on.shape(fd, y, nbasis = 5)
}

}
