% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/spm.R
\name{spm.arl.ewma}
\alias{spm.arl.ewma}
\title{Estimate ARL for EWMA-T-Squared Chart}
\usage{
spm.arl.ewma(
  chart,
  lambda = 0.2,
  ucl = NULL,
  n.sim = 10000,
  max.rl = 5000,
  seed = 42
)
}
\arguments{
\item{chart}{An object of class \code{spm.chart} from \code{\link{spm.phase1}}.}

\item{lambda}{EWMA smoothing parameter (default 0.2).}

\item{ucl}{Upper control limit for the EWMA chart. If \code{NULL}, uses
\code{chart$t2.ucl}.}

\item{n.sim}{Number of simulations (default 10000).}

\item{max.rl}{Maximum run length per simulation (default 5000).}

\item{seed}{Random seed (default 42).}
}
\value{
An object of class \code{spm.arl} with the same structure as
\code{\link{spm.arl}}.
}
\description{
Estimates the in-control Average Run Length for an EWMA-smoothed
T-squared chart via Monte Carlo simulation.
}
\examples{
\donttest{
set.seed(1)
n <- 50; m <- 30
argvals <- seq(0, 1, length.out = m)
X <- matrix(rnorm(n * m), n, m)
fd <- fdata(X, argvals = argvals)
chart <- spm.phase1(fd, ncomp = 3)

arl_ewma <- spm.arl.ewma(chart, lambda = 0.2, n.sim = 1000)
arl_ewma
}

}
\seealso{
\code{\link{spm.arl}} for standard ARL, \code{\link{spm.ewma}}
for EWMA monitoring
}
