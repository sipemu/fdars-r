% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/conformal.R
\name{cv.conformal.classification}
\alias{cv.conformal.classification}
\title{Cross-Conformal (CV+) Classification}
\usage{
cv.conformal.classification(
  fdataobj,
  y,
  newdata,
  covariates.train = NULL,
  covariates.test = NULL,
  ncomp = 3,
  classifier = "lda",
  k.nn = 5,
  score.type = c("lac", "aps"),
  n.folds = 5,
  alpha = 0.1,
  seed = NULL
)
}
\arguments{
\item{fdataobj}{An object of class 'fdata' (training data).}

\item{y}{Class labels (integer vector, 0-indexed).}

\item{newdata}{An object of class 'fdata' (test data).}

\item{covariates.train}{Optional scalar covariates for training.}

\item{covariates.test}{Optional scalar covariates for test.}

\item{ncomp}{Number of FPC components (default 3).}

\item{classifier}{Classifier: "lda", "qda", or "knn" (default "lda").}

\item{k.nn}{k for kNN classifier (default 5).}

\item{score.type}{Nonconformity score: "lac" or "aps" (default "lac").}

\item{n.folds}{Number of folds (default 5).}

\item{alpha}{Miscoverage level (default 0.1).}

\item{seed}{Random seed.}
}
\value{
A list with components:
\describe{
\item{predicted.classes}{Point predictions}
\item{set.sizes}{Size of each prediction set}
\item{average.set.size}{Mean prediction set size}
\item{coverage}{Empirical coverage}
}
}
\description{
Constructs conformal prediction sets using K-fold cross-conformal inference.
}
\examples{
\donttest{
fd <- fdata(matrix(rnorm(500), 50, 10), argvals = seq(0, 1, length.out = 10))
y <- sample(0:1, 50, replace = TRUE)
cp <- cv.conformal.classification(fd[1:40, ], y[1:40], fd[41:50, ])
}

}
