% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/alignment.R
\name{transfer.alignment}
\alias{transfer.alignment}
\title{Transfer Alignment Between Functional Samples}
\usage{
transfer.alignment(source, target, lambda = 0, max.iter = 15, tol = 1e-04)
}
\arguments{
\item{source}{An object of class 'fdata' (the reference sample).}

\item{target}{An object of class 'fdata' (the sample to align).}

\item{lambda}{Regularisation parameter controlling warping smoothness
(default 0).}

\item{max.iter}{Maximum number of iterations (default 15).}

\item{tol}{Convergence tolerance (default 1e-4).}
}
\value{
A list with components:
\describe{
\item{aligned}{fdata of the aligned target curves}
\item{gammas}{fdata of warping functions applied to the target}
\item{source.mean}{fdata of the source Karcher mean}
\item{distances}{numeric vector of elastic distances}
}
}
\description{
Transfer the alignment learned from a source sample of curves to a target
sample. The Karcher mean and warping structure estimated from the source are
used to guide the alignment of the target curves, which is useful when the
target sample is small or noisy.
}
\examples{
\donttest{
set.seed(1)
t <- seq(0, 1, length.out = 50)
X1 <- matrix(0, 20, 50)
for (i in 1:20) X1[i, ] <- sin(2 * pi * t + runif(1, -0.3, 0.3))
X2 <- matrix(0, 5, 50)
for (i in 1:5) X2[i, ] <- sin(2 * pi * t + runif(1, -0.5, 0.5))
source <- fdata(X1, argvals = t)
target <- fdata(X2, argvals = t)
res <- transfer.alignment(source, target)
}
}
\seealso{
\code{\link{karcher.mean}}, \code{\link{elastic.align}}
}
