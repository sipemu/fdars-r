% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/alignment.R
\name{elastic.align.pair.closed}
\alias{elastic.align.pair.closed}
\title{Elastic Alignment of Closed Curves}
\usage{
elastic.align.pair.closed(f1, f2, argvals, lambda = 0)
}
\arguments{
\item{f1}{Numeric vector of the first closed curve's values.}

\item{f2}{Numeric vector of the second closed curve's values.}

\item{argvals}{Numeric vector of the evaluation grid (common to both curves).}

\item{lambda}{Regularisation parameter controlling warping smoothness
(default 0).}
}
\value{
A list with components:
\describe{
\item{gamma}{numeric vector of the estimated warping function}
\item{f2.aligned}{numeric vector of the aligned second curve}
\item{distance}{elastic distance after alignment}
\item{rotation}{the optimal rotation applied}
}
}
\description{
Align two closed curves in the elastic framework. Closed curves satisfy
\eqn{f(0) = f(1)} and the alignment optimises over both reparameterisation
and rotation (circular shift) of the parameter domain.
}
\examples{
\donttest{
t <- seq(0, 1, length.out = 100)
f1 <- sin(2 * pi * t)
f2 <- sin(2 * pi * t + 0.5)
res <- elastic.align.pair.closed(f1, f2, t)
}
}
\references{
Srivastava, A. and Klassen, E. (2016). \emph{Functional and Shape Data
Analysis}. Springer.
}
\seealso{
\code{\link{elastic.distance.closed}},
\code{\link{karcher.mean.closed}}
}
