% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fosr.R
\name{fosr.2d}
\alias{fosr.2d}
\title{2D Function-on-Scalar Regression}
\usage{
fosr.2d(fdataobj, predictors, argvals.s, argvals.t, lambda.s = 0, lambda.t = 0)
}
\arguments{
\item{fdataobj}{An fdata object where each row is a flattened 2D surface
(m1 * m2 grid points).}

\item{predictors}{A matrix of scalar predictors (n x p).}

\item{argvals.s}{Grid points along the s dimension.}

\item{argvals.t}{Grid points along the t dimension.}

\item{lambda.s}{Smoothing penalty in s direction (default 0).}

\item{lambda.t}{Smoothing penalty in t direction (default 0).}
}
\value{
An object of class 'fosr.2d' with components:
\item{intercept}{Intercept surface as fdata}
\item{beta}{Coefficient surfaces as fdata}
\item{fitted}{Fitted surfaces as fdata}
\item{residuals}{Residual surfaces as fdata}
\item{r.squared}{Global R-squared}
\item{r.squared.pointwise}{Pointwise R-squared values}
\item{lambda.s}{Penalty in s direction}
\item{lambda.t}{Penalty in t direction}
\item{gcv}{GCV criterion}
\item{grid}{List with argvals.s, argvals.t, m1, m2}
}
\description{
Fits a penalized regression model where the response is a 2D surface
Y_i(s,t) and predictors are scalar.
}
\examples{
\donttest{
n <- 30; m1 <- 5; m2 <- 5
fd <- fdata(matrix(rnorm(n * m1 * m2), n, m1 * m2))
X <- matrix(rnorm(n * 2), n, 2)
fit <- fosr.2d(fd, X, seq(0, 1, length.out = m1), seq(0, 1, length.out = m2))
}

}
