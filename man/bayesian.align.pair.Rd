% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/alignment.R
\name{bayesian.align.pair}
\alias{bayesian.align.pair}
\title{Bayesian Pairwise Curve Alignment}
\usage{
bayesian.align.pair(
  f1,
  f2,
  argvals,
  n.samples = 2000,
  burn.in = 500,
  step.size = 0.1,
  proposal.variance = 1,
  seed = 42
)
}
\arguments{
\item{f1}{Numeric vector of the first curve's values.}

\item{f2}{Numeric vector of the second curve's values.}

\item{argvals}{Numeric vector of the evaluation grid (common to both curves).}

\item{n.samples}{Number of MCMC samples to draw (default 2000).}

\item{burn.in}{Number of initial samples to discard as burn-in (default 500).}

\item{step.size}{Step size for the MCMC proposal (default 0.1).}

\item{proposal.variance}{Variance of the MCMC proposal distribution
(default 1.0).}

\item{seed}{Random seed for reproducibility (default 42).}
}
\value{
A list with components:
\describe{
\item{gamma.mean}{numeric vector of the posterior mean warping function}
\item{gamma.samples}{matrix of posterior warping function samples
(samples x grid)}
\item{f2.aligned}{numeric vector of the aligned second curve}
\item{acceptance.rate}{the MCMC acceptance rate}
}
}
\description{
Align two curves using a Bayesian framework with MCMC sampling of the warping
function posterior. This provides uncertainty quantification for the alignment
via posterior samples of the warping function.
}
\examples{
\donttest{
t <- seq(0, 1, length.out = 100)
f1 <- sin(2 * pi * t)
f2 <- sin(2 * pi * (t^1.3))
res <- bayesian.align.pair(f1, f2, t, n.samples = 500, burn.in = 100)
}
}
\references{
Cheng, W., Dryden, I.L., and Huang, X. (2016). Bayesian registration of
functions and curves. \emph{Bayesian Analysis}, 11(2):447--475.
}
\seealso{
\code{\link{elastic.align}}
}
