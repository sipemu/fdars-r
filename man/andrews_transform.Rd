% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/andrews.R
\name{andrews_transform}
\alias{andrews_transform}
\title{Andrews Transformation}
\usage{
andrews_transform(X, m = 200)
}
\arguments{
\item{X}{Numeric matrix or data.frame (\eqn{n \times p}). Rows are
observations, columns are variables. Coerced via \code{\link[=as.matrix]{as.matrix()}}.}

\item{m}{Number of grid points on \eqn{[-\pi, \pi]} (default 200).}
}
\value{
An \link{fdata} object (\eqn{n} curves, \eqn{m} grid points) with
additional attributes:
\describe{
\item{\code{andrews_basis}}{The \eqn{m \times p} Fourier basis matrix.}
\item{\code{andrews_varnames}}{Variable names (\code{colnames(X)} or
\code{paste0("V", 1:p)} if \code{NULL}).}
}
}
\description{
Transforms a multivariate dataset into functional data using the Andrews
Fourier expansion. Each row of the input matrix becomes a curve on
\eqn{[-\pi, \pi]}{[-pi, pi]} defined by
\deqn{f_i(t) = x_{i1}/\sqrt{2} + x_{i2}\sin(t) + x_{i3}\cos(t) +
  x_{i4}\sin(2t) + x_{i5}\cos(2t) + \ldots}
}
\details{
The resulting \code{fdata} object carries the Fourier basis matrix and
variable names as attributes, enabling \code{\link[=andrews_loadings]{andrews_loadings()}} to project
FPCA eigenfunctions back to the original variable space.
}
\examples{
X <- scale(iris[, 1:4])
fd <- andrews_transform(X)
plot(fd)
}
\seealso{
\code{\link[=andrews_loadings]{andrews_loadings()}}, \code{\link[=fdata2pc]{fdata2pc()}}
}
