% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/depth.R
\name{streaming.depth}
\alias{streaming.depth}
\title{Streaming Depth for Functional Data}
\usage{
streaming.depth(fdataobj, fdataori = NULL, method = c("FM", "MBD", "BD"), ...)
}
\arguments{
\item{fdataobj}{An object of class 'fdata' to compute depth for.}

\item{fdataori}{An object of class 'fdata' as reference sample.
If NULL, uses fdataobj as reference.}

\item{method}{Streaming depth method: "FM" (Fraiman-Muniz, default),
"MBD" (modified band depth), or "BD" (band depth).}

\item{...}{Additional arguments (ignored).}
}
\value{
Numeric vector of depth values, one per curve in fdataobj.
}
\description{
Compute depth values using a streaming (pre-sorted reference) algorithm.
This approach pre-sorts the reference data at each time point, enabling
O(T log N) depth computation per curve instead of O(N^2 T).
}
\examples{
fd <- fdata(matrix(rnorm(200), 20, 10))
streaming.depth(fd, method = "FM")
}
