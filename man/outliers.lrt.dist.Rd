% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/outliers.R
\name{outliers.lrt.dist}
\alias{outliers.lrt.dist}
\title{LRT Outlier Threshold with Bootstrap Distribution}
\usage{
outliers.lrt.dist(
  fdataobj,
  nb = 200,
  smo = 0.05,
  trim = 0.1,
  seed = NULL,
  percentile = 0.99
)
}
\arguments{
\item{fdataobj}{An object of class 'fdata'.}

\item{nb}{Number of bootstrap replications (default 200).}

\item{smo}{Smoothing parameter for bootstrap noise (default 0.05).}

\item{trim}{Proportion of curves to trim (default 0.1).}

\item{seed}{Random seed for reproducibility.}

\item{percentile}{Percentile for threshold (default 0.99).}
}
\value{
A list with components:
\describe{
\item{threshold}{Threshold at the specified percentile}
\item{boot.distribution}{Sorted bootstrap null distribution of max-distances}
}
}
\description{
Computes the bootstrap threshold AND the full sorted null distribution
for LRT-based outlier detection. The returned distribution enables
per-curve p-value computation: \code{p = (sum(boot_dist >= d) + 1) / (B + 1)}.
}
\examples{
t <- seq(0, 1, length.out = 50)
X <- matrix(0, 30, 50)
for (i in 1:30) X[i, ] <- sin(2*pi*t) + rnorm(50, sd = 0.1)
fd <- fdata(X, argvals = t)
res <- outliers.lrt.dist(fd, nb = 100)
res$threshold
}
