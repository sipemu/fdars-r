% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/alignment.R
\name{elastic.decomposition}
\alias{elastic.decomposition}
\title{Elastic Phase-Amplitude Decomposition}
\usage{
elastic.decomposition(f1, f2, argvals = NULL, lambda = 0)
}
\arguments{
\item{f1}{An 'fdata' object with a single curve, or a numeric vector.}

\item{f2}{An 'fdata' object with a single curve, or a numeric vector.}

\item{argvals}{Evaluation points (required if f1/f2 are numeric vectors).}

\item{lambda}{Penalty weight on warp deviation from identity. Default 0.}
}
\value{
A list with components:
\describe{
\item{gamma}{Warping function}
\item{f_aligned}{Aligned version of f2}
\item{d_amplitude}{Amplitude distance}
\item{d_phase}{Phase distance}
}
}
\description{
Decompose the difference between two curves into amplitude and phase
components using the elastic (Fisher-Rao) framework.
}
\examples{
\donttest{
t <- seq(0, 1, length.out = 50)
f1 <- fdata(matrix(sin(2*pi*t), 1, 50), argvals = t)
f2 <- fdata(matrix(sin(2*pi*t + 0.3), 1, 50), argvals = t)
dec <- elastic.decomposition(f1, f2)
}
}
