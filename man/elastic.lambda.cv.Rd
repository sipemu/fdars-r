% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/alignment.R
\name{elastic.lambda.cv}
\alias{elastic.lambda.cv}
\title{Cross-Validation for Elastic Alignment Regularization Parameter}
\usage{
elastic.lambda.cv(
  fdataobj,
  lambdas = 10^seq(-4, 2, length.out = 20),
  n.folds = 5,
  max.iter = 15,
  tol = 0.001,
  seed = 42
)
}
\arguments{
\item{fdataobj}{An object of class \code{fdata}.}

\item{lambdas}{Numeric vector of candidate lambda values. Default is
\code{10^seq(-4, 2, length.out = 20)}.}

\item{n.folds}{Number of cross-validation folds (default 5).}

\item{max.iter}{Maximum iterations for Karcher mean per fold (default 15).}

\item{tol}{Convergence tolerance (default 1e-3).}

\item{seed}{Random seed for fold assignment (default 42).}
}
\value{
An object of class \code{lambda.cv} with components:
\describe{
\item{best.lambda}{The lambda value with the lowest CV error}
\item{cv.scores}{Numeric vector of CV scores for each lambda}
\item{lambdas}{The candidate lambda values tested}
\item{call}{The matched call}
}
}
\description{
Select the optimal regularization parameter (lambda) for elastic alignment
using K-fold cross-validation. The CV error measures how well the aligned
curves generalize to held-out folds.
}
\examples{
\donttest{
set.seed(1)
t <- seq(0, 1, length.out = 50)
X <- matrix(0, 15, 50)
for (i in 1:15) X[i, ] <- sin(2 * pi * (t - i / 60))
fd <- fdata(X, argvals = t)
cv <- elastic.lambda.cv(fd, lambdas = 10^seq(-2, 1, length.out = 10))
cv
}
}
\references{
Tucker, J.D., Wu, W., and Srivastava, A. (2013). Generative models for
functional data using phase and amplitude separation.
\emph{Computational Statistics & Data Analysis}, 61:50--66.
}
\seealso{
\code{\link{karcher.mean}}, \code{\link{elastic.align}}
}
