% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/conformal.R
\name{conformal.generic.regression}
\alias{conformal.generic.regression}
\title{Generic Conformal Regression}
\usage{
conformal.generic.regression(
  model,
  fdataobj,
  y,
  newdata,
  scalar.train = NULL,
  scalar.test = NULL,
  cal.fraction = 0.25,
  alpha = 0.1,
  seed = NULL
)
}
\arguments{
\item{model}{A fitted \code{fregre.lm} model object.}

\item{fdataobj}{An object of class 'fdata' (training data).}

\item{y}{Response vector (training).}

\item{newdata}{An object of class 'fdata' (test data).}

\item{scalar.train}{Optional scalar covariates for training.}

\item{scalar.test}{Optional scalar covariates for test.}

\item{cal.fraction}{Fraction of data for calibration (default 0.25).}

\item{alpha}{Miscoverage level (default 0.1).}

\item{seed}{Random seed.}
}
\value{
Same as \code{\link{conformal.fregre.lm}}.
}
\description{
Split-conformal prediction intervals using a pre-fitted fregre.lm model.
Uses the model's FPCA components for fast prediction without refitting.
}
\examples{
\donttest{
fd <- fdata(matrix(rnorm(500), 50, 10), argvals = seq(0, 1, length.out = 10))
y <- rnorm(50)
model <- fregre.lm(fd, y)
cp <- conformal.generic.regression(model, fd, y, fd[1:10, ])
}

}
