% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/alignment.R
\name{shape.confidence.interval}
\alias{shape.confidence.interval}
\title{Bootstrap Confidence Bands for Elastic Karcher Mean}
\usage{
shape.confidence.interval(
  fdataobj,
  n.bootstrap = 200,
  confidence.level = 0.95,
  lambda = 0,
  max.iter = 15,
  tol = 1e-04,
  seed = 42
)
}
\arguments{
\item{fdataobj}{An object of class 'fdata'.}

\item{n.bootstrap}{Number of bootstrap replicates (default 200).}

\item{confidence.level}{Confidence level for the bands (default 0.95).}

\item{lambda}{Regularisation parameter controlling warping smoothness
(default 0).}

\item{max.iter}{Maximum number of Karcher mean iterations per replicate
(default 15).}

\item{tol}{Convergence tolerance for each Karcher mean computation
(default 1e-4).}

\item{seed}{Random seed for reproducibility (default 42).}
}
\value{
An object of class 'shape.ci' with components:
\describe{
\item{mean}{fdata of the Karcher mean curve}
\item{lower}{numeric vector of lower confidence band values}
\item{upper}{numeric vector of upper confidence band values}
\item{confidence.level}{the confidence level used}
\item{n.bootstrap}{the number of bootstrap replicates used}
\item{argvals}{the evaluation grid}
}
}
\description{
Construct pointwise bootstrap confidence bands for the elastic Karcher mean
by resampling curves and recomputing the mean for each bootstrap replicate.
}
\examples{
\donttest{
set.seed(1)
t <- seq(0, 1, length.out = 50)
X <- matrix(0, 20, 50)
for (i in 1:20) X[i, ] <- sin(2 * pi * t + runif(1, -0.2, 0.2))
fd <- fdata(X, argvals = t)
ci <- shape.confidence.interval(fd, n.bootstrap = 50)
}
}
\references{
Tucker, J.D., Wu, W., and Srivastava, A. (2013). Generative models for
functional data using phase and amplitude separation.
\emph{Computational Statistics & Data Analysis}, 61:50--66.
}
\seealso{
\code{\link{karcher.mean}}
}
