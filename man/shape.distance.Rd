% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/alignment.R
\name{shape.distance}
\alias{shape.distance}
\title{Shape Distance Between Two Curves}
\usage{
shape.distance(
  f1,
  f2,
  argvals = NULL,
  quotient = c("reparameterization", "translation", "scale"),
  lambda = 0
)
}
\arguments{
\item{f1}{Numeric vector (first curve).}

\item{f2}{Numeric vector (second curve).}

\item{argvals}{Numeric vector of evaluation points. If \code{NULL},
defaults to \code{seq(0, 1, length.out = length(f1))}.}

\item{quotient}{Character: the quotient space. One of
\code{"reparameterization"}, \code{"translation"}, or \code{"scale"}.}

\item{lambda}{Regularization parameter (default 0).}
}
\value{
A list with components:
\describe{
\item{distance}{Shape distance}
\item{gamma}{Optimal warping function}
\item{f2.aligned}{Aligned version of f2}
}
}
\description{
Compute the elastic shape distance between two curves in a specified
quotient space. The shape distance factors out the specified
nuisance transformations (reparameterization, translation, scale).
}
\examples{
\donttest{
t <- seq(0, 1, length.out = 50)
f1 <- sin(2 * pi * t)
f2 <- 2 * sin(2 * pi * t^1.5) + 1
d <- shape.distance(f1, f2, argvals = t, quotient = "scale")
d$distance
}
}
\references{
Srivastava, A. and Klassen, E. (2016). \emph{Functional and Shape Data
Analysis}. Springer.
}
\seealso{
\code{\link{shape.representative}}, \code{\link{shape.mean}},
\code{\link{shape.distance.matrix}}
}
