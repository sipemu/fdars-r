# Conformal prediction for logistic regression

Conformal prediction for logistic regression

## Usage

``` r
conformal_logistic_rust(
  data,
  y,
  test_data,
  scalar_train,
  scalar_test,
  ncomp,
  max_iter,
  tol,
  score_type,
  cal_fraction,
  alpha,
  seed
)
```
