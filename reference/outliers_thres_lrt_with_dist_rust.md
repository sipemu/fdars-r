# LRT outlier threshold with full bootstrap distribution

LRT outlier threshold with full bootstrap distribution

## Usage

``` r
outliers_thres_lrt_with_dist_rust(
  data,
  `_argvals`,
  nb,
  smo,
  trim,
  seed,
  percentile
)
```
