# Jackknife+ regression using nonparametric kernel regression

Jackknife+ regression using nonparametric kernel regression

## Usage

``` r
jackknife_plus_fregre_np_rust(
  data,
  y,
  test_data,
  argvals,
  scalar_train,
  scalar_test,
  h_func,
  h_scalar,
  alpha
)
```
