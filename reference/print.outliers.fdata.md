# Print method for outliers.fdata objects

Print method for outliers.fdata objects

## Usage

``` r
# S3 method for class 'outliers.fdata'
print(x, ...)
```

## Arguments

- x:

  An outliers.fdata object.

- ...:

  Additional arguments (ignored).

## Value

Invisibly returns the input object `x`.
