# Karcher median (Weiszfeld algorithm in elastic metric)

Karcher median (Weiszfeld algorithm in elastic metric)

## Usage

``` r
alignment_karcher_median(data, argvals, max_iter, tol, lambda, trim_fraction)
```
