# Gaussian generative model: sample random curves from aligned data

Gaussian generative model: sample random curves from aligned data

## Usage

``` r
alignment_gauss_model(
  mean_curve,
  mean_srsf,
  gammas,
  aligned_data,
  argvals,
  ncomp,
  n_samples,
  seed
)
```
