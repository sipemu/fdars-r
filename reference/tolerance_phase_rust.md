# Phase tolerance band (warping variation)

Phase tolerance band (warping variation)

## Usage

``` r
tolerance_phase_rust(
  data,
  argvals,
  ncomp,
  nb,
  coverage,
  band_type,
  max_iter,
  seed
)
```
