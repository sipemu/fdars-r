# Predict from elastic logistic regression

Predict from elastic logistic regression

## Usage

``` r
predict_elastic_logistic_rust(
  alpha,
  beta,
  probabilities,
  predicted_classes,
  accuracy,
  loss,
  gammas_data,
  aligned_srsfs_data,
  n_iter,
  new_data,
  argvals
)
```
