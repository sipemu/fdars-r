# Autoperiod: Hybrid FFT + ACF period detection with gradient ascent refinement Returns period, confidence, FFT power, ACF validation score, and candidates

Autoperiod: Hybrid FFT + ACF period detection with gradient ascent
refinement Returns period, confidence, FFT power, ACF validation score,
and candidates

## Usage

``` r
seasonal_autoperiod(data, argvals, n_candidates, gradient_steps)
```
