# Horizontal (phase) FPCA after elastic alignment

Horizontal (phase) FPCA after elastic alignment

## Usage

``` r
elastic_horiz_fpca_rust(
  aligned_data,
  gammas,
  mean_data,
  mean_srsf,
  aligned_srsfs,
  argvals,
  ncomp
)
```
