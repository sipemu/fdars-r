# Joint FPCA (amplitude + phase)

Joint FPCA (amplitude + phase)

## Usage

``` r
elastic_joint_fpca_rust(
  aligned_data,
  gammas_data,
  mean_curve,
  mean_srsf,
  aligned_srsfs,
  argvals,
  ncomp
)
```
