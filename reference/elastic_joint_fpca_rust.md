# Joint (amplitude + phase) FPCA after elastic alignment

Joint (amplitude + phase) FPCA after elastic alignment

## Usage

``` r
elastic_joint_fpca_rust(
  aligned_data,
  gammas,
  mean_data,
  mean_srsf,
  aligned_srsfs,
  argvals,
  ncomp
)
```
