# Predict from elastic regression

Predict from elastic regression

## Usage

``` r
predict_elastic_regression_rust(
  alpha,
  beta,
  fitted_values,
  residuals,
  sse,
  r_squared,
  gammas_data,
  aligned_srsfs_data,
  n_iter,
  new_data,
  argvals
)
```
