# Plot Shape Mean

Displays the shape mean curve with aligned input curves in the
background.

## Usage

``` r
# S3 method for class 'shape.mean'
plot(x, ...)
```

## Arguments

- x:

  An object of class `shape.mean`.

- ...:

  Additional arguments (ignored).

## Value

A ggplot object.
