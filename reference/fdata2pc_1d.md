# Perform functional PCA via SVD on centered data Returns: singular values, rotation matrix (loadings), scores, mean

Perform functional PCA via SVD on centered data Returns: singular
values, rotation matrix (loadings), scores, mean

## Usage

``` r
fdata2pc_1d(data, ncomp, `_lambda`)
```
