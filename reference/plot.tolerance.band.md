# Plot Tolerance Band

Plot Tolerance Band

## Usage

``` r
# S3 method for class 'tolerance.band'
plot(x, show.data = TRUE, ...)
```

## Arguments

- x:

  An object of class 'tolerance.band'.

- show.data:

  Logical; if TRUE (default), overlay the original curves.

- ...:

  Additional arguments (ignored).

## Value

A ggplot object (invisibly).
