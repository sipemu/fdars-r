# Perform PLS via NIPALS algorithm Returns: weights, scores, loadings

Perform PLS via NIPALS algorithm Returns: weights, scores, loadings

## Usage

``` r
fdata2pls_1d(data, y, ncomp, `_lambda`)
```
