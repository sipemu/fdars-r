# Estimate mean function for irregular data using kernel smoothing

Estimate mean function for irregular data using kernel smoothing

## Usage

``` r
irreg_mean_kernel(
  offsets,
  argvals,
  values,
  target_argvals,
  bandwidth,
  kernel_type
)
```
