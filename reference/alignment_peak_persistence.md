# Peak persistence diagram across a sweep of lambda values

Peak persistence diagram across a sweep of lambda values

## Usage

``` r
alignment_peak_persistence(data, argvals, lambdas, max_iter, tol)
```
