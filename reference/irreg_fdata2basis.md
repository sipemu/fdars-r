# Fit basis functions to irregular functional data Each curve is individually fitted via least squares at its own observation points basis_type: 0 = bspline, 1 = fourier

Fit basis functions to irregular functional data Each curve is
individually fitted via least squares at its own observation points
basis_type: 0 = bspline, 1 = fourier

## Usage

``` r
irreg_fdata2basis(offsets, argvals, values, nbasis, basis_type)
```
