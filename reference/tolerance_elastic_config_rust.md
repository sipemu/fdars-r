# Elastic tolerance band with full config (amplitude + phase)

Elastic tolerance band with full config (amplitude + phase)

## Usage

``` r
tolerance_elastic_config_rust(
  data,
  argvals,
  ncomp_amplitude,
  ncomp_phase,
  nb,
  coverage,
  band_type,
  max_iter,
  tol,
  seed
)
```
