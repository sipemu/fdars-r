# Penalized elastic alignment with configurable penalty type

Penalized elastic alignment with configurable penalty type

## Usage

``` r
alignment_elastic_pair_penalized_rust(f1, f2, argvals, penalty_type, lambda)
```
