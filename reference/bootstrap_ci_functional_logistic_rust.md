# Bootstrap CI for functional logistic model beta(t)

Bootstrap CI for functional logistic model beta(t)

## Usage

``` r
bootstrap_ci_functional_logistic_rust(
  data,
  y,
  scalar_covariates,
  ncomp,
  n_boot,
  alpha,
  seed,
  max_iter,
  tol
)
```
