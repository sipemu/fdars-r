# Iterative Phase I chart construction.

Iterative Phase I chart construction.

## Usage

``` r
spm_phase1_iterative_rust(
  data,
  argvals,
  ncomp,
  alpha,
  tuning_fraction,
  seed,
  max_iterations,
  max_removal_fraction
)
```
