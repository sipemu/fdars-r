# Bootstrap shape confidence intervals for the elastic Karcher mean

Bootstrap shape confidence intervals for the elastic Karcher mean

## Usage

``` r
alignment_shape_ci(
  data,
  argvals,
  n_bootstrap,
  confidence_level,
  lambda,
  max_iter,
  tol,
  seed
)
```
