# Model Explainability

Explainability tools for functional regression models (fregre.lm and
fregre.logistic). Includes global explanations (PDP, ALE, Sobol), local
explanations (SHAP, LIME, anchors, counterfactuals), diagnostics, and
calibration metrics for logistic models.

## Usage

``` r
.fit_args(model)
```
