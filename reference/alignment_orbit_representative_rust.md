# Compute the orbit representative of a curve in a quotient space

Compute the orbit representative of a curve in a quotient space

## Usage

``` r
alignment_orbit_representative_rust(f, argvals, quotient)
```
