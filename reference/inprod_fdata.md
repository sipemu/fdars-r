# Inner product of two functional data objects \<f, g\> = integral(f(t) \* g(t) dt)

Inner product of two functional data objects \<f, g\> = integral(f(t) \*
g(t) dt)

## Usage

``` r
inprod_fdata(data1, data2, argvals)
```
