# Elastic partial matching of a template within a longer target curve

Elastic partial matching of a template within a longer target curve

## Usage

``` r
alignment_partial_match(
  template,
  target,
  argvals_template,
  argvals_target,
  lambda,
  min_span
)
```
