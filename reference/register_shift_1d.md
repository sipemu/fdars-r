# Shift registration: find optimal horizontal shift for each curve to align with a target (usually the mean)

Shift registration: find optimal horizontal shift for each curve to
align with a target (usually the mean)

## Usage

``` r
register_shift_1d(data, target, argvals, max_shift)
```
