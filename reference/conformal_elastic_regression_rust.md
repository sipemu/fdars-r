# Conformal prediction for elastic regression

Conformal prediction for elastic regression

## Usage

``` r
conformal_elastic_regression_rust(
  data,
  y,
  test_data,
  argvals,
  ncomp_beta,
  lambda,
  cal_fraction,
  alpha,
  seed
)
```
