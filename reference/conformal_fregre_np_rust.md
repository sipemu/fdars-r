# Conformal prediction for nonparametric functional regression

Conformal prediction for nonparametric functional regression

## Usage

``` r
conformal_fregre_np_rust(
  data,
  y,
  test_data,
  argvals,
  scalar_train,
  scalar_test,
  h_func,
  h_scalar,
  cal_fraction,
  alpha,
  seed
)
```
