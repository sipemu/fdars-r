# Joint Gaussian generative model preserving amplitude-phase correlation

Joint Gaussian generative model preserving amplitude-phase correlation

## Usage

``` r
alignment_joint_gauss_model(
  mean_curve,
  mean_srsf,
  gammas,
  aligned_data,
  argvals,
  ncomp,
  n_samples,
  balance_c,
  seed
)
```
