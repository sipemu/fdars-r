# Multi-resolution elastic alignment (coarse DP + fine gradient refinement)

Multi-resolution elastic alignment (coarse DP + fine gradient
refinement)

## Usage

``` r
alignment_multires_pair(
  f1,
  f2,
  argvals,
  coarsen_factor,
  n_refine_steps,
  step_size,
  lambda
)
```
