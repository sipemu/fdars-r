# Plot method for fequiv.test

Creates a ggplot showing the mean difference curve, simultaneous
confidence band, and equivalence margins.

## Usage

``` r
# S3 method for class 'fequiv.test'
plot(x, ...)
```

## Arguments

- x:

  An `fequiv.test` object.

- ...:

  Additional arguments (ignored).

## Value

A ggplot object (invisibly).
