# Elastic tolerance band (alignment + FPCA)

Elastic tolerance band (alignment + FPCA)

## Usage

``` r
tolerance_elastic(
  data,
  argvals,
  ncomp,
  nb,
  coverage,
  band_type,
  max_iter,
  seed
)
```
