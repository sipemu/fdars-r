# CV-conformal regression using nonparametric kernel regression

CV-conformal regression using nonparametric kernel regression

## Usage

``` r
cv_conformal_fregre_np_rust(
  data,
  y,
  test_data,
  argvals,
  scalar_train,
  scalar_test,
  h_func,
  h_scalar,
  n_folds,
  alpha,
  seed
)
```
