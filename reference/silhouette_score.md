# Compute silhouette score for clustering Returns the mean silhouette coefficient across all samples

Compute silhouette score for clustering Returns the mean silhouette
coefficient across all samples

## Usage

``` r
silhouette_score(dist_matrix, clusters)
```
