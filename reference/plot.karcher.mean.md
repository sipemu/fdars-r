# Plot Karcher Mean Results

Plot Karcher Mean Results

## Usage

``` r
# S3 method for class 'karcher.mean'
plot(x, type = c("mean", "aligned", "warps"), ...)
```

## Arguments

- x:

  An object of class 'karcher.mean'.

- type:

  Character: "mean" (default), "aligned", or "warps".

- ...:

  Additional arguments (ignored).

## Value

A ggplot object (invisibly).
