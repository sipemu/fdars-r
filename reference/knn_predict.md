# Kernel prediction with fixed bandwidth for prediction on new data

Kernel prediction with fixed bandwidth for prediction on new data

## Usage

``` r
knn_predict(dist_matrix, response, k, local_k)
```
