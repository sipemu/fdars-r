# Local Linear Regression smoother matrix Uses weighted least squares with degree-1 polynomial

Local Linear Regression smoother matrix Uses weighted least squares with
degree-1 polynomial

## Usage

``` r
s_llr(argvals, h, kernel_type, weights, cv)
```
