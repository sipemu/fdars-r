# Conformal prediction for elastic logistic regression

Conformal prediction for elastic logistic regression

## Usage

``` r
conformal_elastic_logistic_rust(
  data,
  y,
  test_data,
  argvals,
  lambda,
  score_type,
  cal_fraction,
  alpha,
  seed
)
```
