# Compute bootstrap threshold for LRT outlier detection Highly parallelized across bootstrap iterations

Compute bootstrap threshold for LRT outlier detection Highly
parallelized across bootstrap iterations

## Usage

``` r
outliers_thres_lrt(data, argvals, nb, smo, trim, seed, percentile)
```
