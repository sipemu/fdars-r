# Detect seasonality changes (onset/cessation)

Detect seasonality changes (onset/cessation)

## Usage

``` r
seasonal_detect_changes(
  data,
  argvals,
  period,
  threshold,
  window_size,
  min_duration
)
```
