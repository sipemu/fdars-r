# Kernel Functional Spatial Depth (KFSD) for 2D functional data Implements the RKHS-based formulation matching fda.usc

Kernel Functional Spatial Depth (KFSD) for 2D functional data Implements
the RKHS-based formulation matching fda.usc

## Usage

``` r
depth_kfsd_2d(fdataobj, fdataori, `_m1`, `_m2`, h)
```
