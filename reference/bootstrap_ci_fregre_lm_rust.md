# Bootstrap CI for functional linear model beta(t)

Bootstrap CI for functional linear model beta(t)

## Usage

``` r
bootstrap_ci_fregre_lm_rust(
  data,
  y,
  scalar_covariates,
  ncomp,
  n_boot,
  alpha,
  seed
)
```
