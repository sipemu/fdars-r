# Elastic alignment with automatic landmark detection

Elastic alignment with automatic landmark detection

## Usage

``` r
alignment_with_landmarks(
  f1,
  f2,
  argvals,
  kind,
  min_prominence,
  expected_count,
  lambda
)
```
