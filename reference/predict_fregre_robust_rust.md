# Predict from robust (L1/Huber) regression

Predict from robust (L1/Huber) regression

## Usage

``` r
predict_fregre_robust_rust(
  intercept,
  coefficients,
  fpca_mean,
  fpca_rotation_data,
  fpca_rotation_nrow,
  fpca_rotation_ncol,
  ncomp,
  new_data,
  new_scalar
)
```
