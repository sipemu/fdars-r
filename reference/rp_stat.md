# Compute random projection statistics (parallelized over projections)

Compute random projection statistics (parallelized over projections)

## Usage

``` r
rp_stat(proj_x_ord, residuals, n_proj)
```
