# LRT-based outlier detection Returns indices of detected outliers

LRT-based outlier detection Returns indices of detected outliers

## Usage

``` r
outliers_lrt(data, argvals, nb, smo, trim, seed, percentile)
```
