# Detect peaks in functional data using Fourier basis smoothing

Detect peaks in functional data using Fourier basis smoothing

## Usage

``` r
seasonal_detect_peaks(
  data,
  argvals,
  min_distance,
  min_prominence,
  smooth_first,
  smooth_nbasis
)
```
