# Lambda cross-validation for elastic alignment regularisation

Lambda cross-validation for elastic alignment regularisation

## Usage

``` r
alignment_lambda_cv_rust(data, argvals, lambdas, n_folds, max_iter, tol, seed)
```
