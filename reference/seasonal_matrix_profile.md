# Matrix Profile for motif discovery and period detection Uses STOMP algorithm for efficient computation

Matrix Profile for motif discovery and period detection Uses STOMP
algorithm for efficient computation

## Usage

``` r
seasonal_matrix_profile(data, subsequence_length, exclusion_zone)
```
