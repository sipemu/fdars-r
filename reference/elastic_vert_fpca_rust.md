# Vertical FPCA on elastic-aligned data

Vertical FPCA on elastic-aligned data

## Usage

``` r
elastic_vert_fpca_rust(
  aligned_data,
  gammas_data,
  mean_curve,
  mean_srsf,
  aligned_srsfs,
  argvals,
  ncomp
)
```
