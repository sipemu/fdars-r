# Vertical (amplitude) FPCA after elastic alignment

Vertical (amplitude) FPCA after elastic alignment

## Usage

``` r
elastic_vert_fpca_rust(
  aligned_data,
  gammas,
  mean_data,
  mean_srsf,
  aligned_srsfs,
  argvals,
  ncomp
)
```
