# FPCA-based changepoint detection

FPCA-based changepoint detection

## Usage

``` r
elastic_fpca_changepoint_rust(
  data,
  argvals,
  pca_method,
  ncomp,
  lambda,
  max_iter,
  n_mc,
  seed
)
```
