# Bayesian pairwise alignment via pCN MCMC on the Hilbert sphere

Bayesian pairwise alignment via pCN MCMC on the Hilbert sphere

## Usage

``` r
alignment_bayesian_pair(
  f1,
  f2,
  argvals,
  n_samples,
  burn_in,
  step_size,
  proposal_variance,
  seed
)
```
