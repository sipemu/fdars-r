# Robust (trimmed) Karcher mean in elastic metric

Robust (trimmed) Karcher mean in elastic metric

## Usage

``` r
alignment_robust_karcher_mean(
  data,
  argvals,
  max_iter,
  tol,
  lambda,
  trim_fraction
)
```
