# Local Polynomial Regression smoother matrix Solves (p+1)×(p+1) weighted least squares system for each point

Local Polynomial Regression smoother matrix Solves (p+1)×(p+1) weighted
least squares system for each point

## Usage

``` r
s_lpr(argvals, h, p, kernel_type, weights, cv)
```
