# Detect seasonality changes with automatic threshold

Detect seasonality changes with automatic threshold

## Usage

``` r
seasonal_detect_changes_auto(
  data,
  argvals,
  period,
  threshold_method,
  threshold_value,
  window_size,
  min_duration
)
```
